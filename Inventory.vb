Imports System.Math
Imports topology
Sub Inventory
Function x1y1z1t1
const x1 = "!!!!!!!!"
const y1 = "@@@@@@@@"
const z1 = "########"
const t1 = "$$$$$$$$"
   do (x1 * y1 * z1) / t1;
   for(x1=0.1;x1<=2;++x1)
   for(y1=0.2;y1<=2;++y1)
   for(z1=0.3;z1<=2;++z1)
   for(t1=0.4;t1<=2;++t1)
   while(x1 % y1 % z1)
   goto Inventory
End Function
Function x2y2z2t2
const x2 = "~~~~~~~~"
const y2 = "&&&&&&&&"
const z2 = "||||||||"
const t2 = "========"
   do (x2 * y2 * z2) / t2;
   for(x2=0.6;x2<=4;x2++)
   for(y2=0.7;y2<=4;y2++)
   for(z2=0.8;z2<=4;z2++)
   for(t2=0.9;t2<=4;t2++)
   while(x2 % y2 % z2)
    goto Inventory
End Function
Function x3y3z3t3
const x3 = "<><><><>"
const y3 = "()()()()"
const z3 = "[][][][]"
const t3 = "{}{}{}{}"
   do (x3 * y3 * z3) / t3;
   for(x3=1.3;x3<=8;++x3)
   for(y3=1.4;y3<=8;++y3)
   for(z3=1.5;z3<=8;++z3)
   for(t3=1.6;t3<=8;++t3)
   while(x3 % y3 %z3)
    goto Inventory
End Function 
Function x4y4z4t4
const x4 = "++++++++"
const y4 = "--------"
const z4 = "********"
const t4 = "%%%%%%%%"
   do (x4 * y4 * z4) / t4;
   for(x4=1.7;x4<=16;x4++)
   for(y4=1.8;y4<=16;y4++)
   for(z4=1.9;z4<=16;z4++)
   for(t4=2.0;t4<=16;t4++)
   while(x4 % y4 % z4)
    goto Inventory
End Function
End Sub